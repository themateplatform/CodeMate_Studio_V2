# Environment Variables for CodeMate Studio Deployments
# Copy and customize for each deployment type

# =============================================================================
# COMMON VARIABLES (All Deployment Types)
# =============================================================================

# Application Settings
NODE_ENV=production
PORT=5000
DEPLOYMENT_TYPE=autoscale

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-this

# =============================================================================
# DATABASE & BACKEND SERVICES
# =============================================================================

# Supabase Configuration (Required for all)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE=your-service-role-key

# PostgreSQL Database URL (Auto-provided by Supabase)
DATABASE_URL=postgresql://user:password@host:port/database

# =============================================================================
# AUTOSCALE & ENTERPRISE DEPLOYMENTS ONLY
# =============================================================================

# OpenAI Integration (Optional)
OPENAI_API_KEY=sk-your-openai-api-key

# GitHub Integration (Optional)
GITHUB_CLIENT_ID=your-github-oauth-app-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-secret

# WebSocket Configuration (Enterprise only)
WS_PORT=8080
WS_ENABLED=true

# =============================================================================
# STATIC DEPLOYMENT ONLY
# =============================================================================

# Client-side Environment Variables (Must be prefixed with VITE_)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_BASE_URL=https://your-backend-api.replit.app

# Static Build Configuration
VITE_DEPLOYMENT_TYPE=static
VITE_APP_NAME="CodeMate Studio"
VITE_APP_VERSION=1.0.0

# =============================================================================
# SCHEDULED DEPLOYMENT ONLY
# =============================================================================

# Job Configuration
JOB_TIMEOUT_MINUTES=30
JOB_RETRY_ATTEMPTS=3
JOB_NOTIFICATION_EMAIL=admin@example.com

# Analytics Configuration
ANALYTICS_RETENTION_DAYS=90
BACKUP_RETENTION_DAYS=30

# =============================================================================
# DEVELOPMENT OVERRIDES
# =============================================================================

# Development-specific settings (not used in production)
# LOG_LEVEL=debug
# MOCK_EXTERNAL_APIS=true
# DEV_CORS_ORIGIN=http://localhost:3000

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. NEVER commit this file with real secrets
# 2. Use Replit's secret management for production
# 3. Rotate keys regularly
# 4. Use different keys for each environment
# 5. Limit permissions for service accounts

# =============================================================================
# DEPLOYMENT TYPE REQUIREMENTS
# =============================================================================

# Autoscale Deployment:
# - All common variables
# - Optional: OpenAI, GitHub integrations
# - Auto-scaling based on traffic
# - Best for: Production web apps

# Static Deployment:
# - VITE_ prefixed variables only
# - No server-side secrets
# - CDN distribution
# - Best for: Frontend-only demos

# Scheduled Deployment:
# - Common variables + job config
# - Database access required
# - No web interface
# - Best for: Background tasks

# Enterprise Deployment:
# - All variables
# - WebSocket configuration
# - Dedicated resources
# - Best for: High-traffic applications